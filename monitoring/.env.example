# monitoring/.env 로 복사해서 쓴다. compose 가 자동으로 읽는다.
#   cp monitoring/.env.example monitoring/.env
#
# .env 는 .gitignore 에 있어 커밋되지 않는다. 비밀번호를 넣어도 된다.

# Redis 주소. 같은 VPC 안이라면 사설 IP 가 가장 확실하다.
# 퍼블릭 DNS 이름이 외부 경로를 가리켜 닿지 않는 경우가 있다.
REDIS_ADDR=172.31.x.x:6379

# 비밀번호가 걸려 있으면 지정한다. 없으면 비워 둔다.
#REDIS_PASSWORD=

#REDIS_DB=0
#REDIS_KEY=liz.stats.server.modbus.network.traffic

# Grafana 관리자 비밀번호. 지정하면 기동할 때마다 강제로 다시 설정한다.
#GRAFANA_PASSWORD=

# Grafana 바인드 주소. 127.0.0.1 로 두면 외부에서 접속할 수 없다.
#GRAFANA_BIND=0.0.0.0
