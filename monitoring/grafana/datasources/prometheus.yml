apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    # uid 는 대시보드 패널이 참조하는 값이다. 지정하지 않으면 Grafana 가
    # 임의로 만들어 버려서 패널이 "Datasource not found" 로 비어 버린다.
    uid: PROM
    access: proxy
    url: http://prometheus:9090
    isDefault: true
